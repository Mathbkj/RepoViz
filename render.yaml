services:
  - type: web
    name: repo-viz-api
    env: node
    plan: free
    branch: main
    buildCommand: npm install && npm run build
    startCommand: node backend/dist/index.js
    healthCheckPath: /
    envVars:
      - key: NODE_ENV
        value: production
